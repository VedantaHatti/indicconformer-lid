# GPU overlay — install AFTER requirements.txt
#
#   pip uninstall -y onnxruntime
#   pip install -r requirements-gpu.txt
#
# Or use: make setup  (choose gpu)
#
# Requires a working NVIDIA driver. This service uses ONNX Runtime CUDA for
# encoder/CTC; PyTorch stays on CPU for the small preprocessor.

onnxruntime-gpu==1.19.2
