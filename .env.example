MODEL_DIR=model
# Default is cpu (safe). Use cuda only after: make setup → choose gpu
LID_DEVICE=cpu
LID_MARGIN_THRESHOLD=0.050965
LID_CANDIDATE_LANGUAGES=hi,kn,mr,ta,te
LID_HOST=0.0.0.0
LID_PORT=8007
# HF_TOKEN=   # optional; used only by make setup / scripts/setup_model.py
